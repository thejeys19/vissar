# Production environment variables
# VERCEL_OIDC_TOKEN removed — set via Vercel environment variable UI only, never in files
# All secrets must be configured in Vercel's dashboard, not committed here
